type int is integer
type bool is boolean
type float is real
type vector is array [5] int
type vr is vector
var a : vr
var b : float

routine main(m : integer) is
    b := 5.2
    var i is 1
    while i <= 5 loop
        a[i] := b
        b := b + 0.5
    end
end
